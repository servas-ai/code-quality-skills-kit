---
description: D1 sub-skill (derivable cache — real template in code-quality-checker.md § Skill kit)
weight: see master
---

# This file is a derivable cache.

The authoritative template for d1 lives in `code-quality-checker.md` § Skill kit.

Sub-agents spawned for this skill should:
1. Read `code-quality-checker.md` § Shared contract.
2. Read `code-quality-checker.md` § Skill kit:d1 for the CHECKS block.
3. Execute the 4-step contract (CLAIM → AUDIT → REPORT → CHECK OUT) against `audit-reports/<run-id>/_run.json` + `_findings.jsonl`.
