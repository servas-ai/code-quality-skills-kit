# cqc.config.yaml — generated by cqc-budget @ 2026-05-01T21:24:35Z
# https://github.com/servas-ai/code-quality-skills-kit

cli_agents:
  enabled: [codex, gemini, claude, opencode]
  skill_assignment:
    claude:   [d4, d6, d13]
    gemini:   [d12, d8, d9]
    opencode: [d1, d3, d11, d14, d15]
    codex:    [d2, d5, d7, d10]

# Per-CLI max-% threshold (% of weekly limit)
budget_thresholds:
  claude: 75%
  gemini: 75%
  opencode: 75%
  codex: 75%

# Hard stop if any CLI hits its threshold during the run
stop_on_threshold: true

# Reset timestamps (when each CLI's quota window resets)
# Probed @ 2026-05-01T21:24:35Z — re-run cqc-budget to refresh
reset_info:
  claude:
    label: "weekly @ Mon 00:00 UTC"
    in_seconds: 182128
  gemini:
    label: "daily 1000-req @ 08:00 UTC"
    in_seconds: 38127
  opencode:
    label: "no usage cap (BYOK)"
    in_seconds: 0
  codex:
    label: "5h window + weekly cap"
    in_seconds: 18000
